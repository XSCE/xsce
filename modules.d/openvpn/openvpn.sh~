function openvpn()
{
	case "$1" in
    "yes")
        $YUM_CMD  openvpn 2>&1 | tee -a $LOG
        if [ $? -ne 0 ] ; then
            echo "\n\nYum returned an error\n\n" | tee -a $LOG
            exit $YUMERROR
        fi
        touch $SETUPSTATEDIR/openvpn
        #execute the setup script
        ;;
    "no")
        rm $SETUPSTATEDIR/openvpn
        ;;
    esac
}


