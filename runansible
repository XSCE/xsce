#!/bin/bash
if [ x"$1" == x"xo" ]
then
	ansible-playbook -i ansible_hosts dxs-xo.yml --connection=local 
	echo "Please reboot!"
else
        ansible-playbook -i ansible_hosts dxs.yml --connection=local 
fi
