## XS Config override
##
## This file has an ".in" template - for details see
##  see /usr/share/doc/xs-config-<version>/README 
#
# Point dhcpd to the XS-specific config
DHCPDARGS='-cf /etc/dhcpd-xs.conf'

# the dhcpd-xs.conf file is 'made' based on
# a couple of files - complain if we don't have them
if [ ! -e /etc/sysconfig/xs_domain_name ];then
    echo "You must run domain_config before DHCPd can run"
    exit 1
fi
pushd /etc
make -B -f xs-config.make dhcpd-xs.conf
popd
