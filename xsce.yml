---
- hosts: all
  sudo: yes
  vars:
  - xsce_hostname: schoolserver
  - xsce_domain: lan 
  vars_files:
  - vars/default_vars.yml
  roles:
      - common
      - network
      - gateway
      - core
