#!/bin/bash

WAN=`/sbin/route -n | awk '{if($4=="UG")print $8}' | tr -d ' '`
LAN=`/sbin/ip link show  | grep mtu | awk -F":" '{print \$2}' | grep -v -e lo -e $WAN| tr -d ' ' ` 

WAN_MAC=`/sbin/ip addr show $WAN | /bin/awk '/link\/ether/ { print \$2}' | tr [:lower:] [:upper:]`
LAN_MAC=`/sbin/ip addr show $LAN | /bin/awk '/link\/ether/ { print \$2}' | tr [:lower:] [:upper:] | head -n1`


if [ -f /proc/device-tree/mfg-data/MN ]
	then
	XO_VERSION=`cat /proc/device-tree/mfg-data/MN`
	else
	XO_VERSION="none"
fi

if [ x"$XO_VERSION" == "XO-4" ]
	then
	LAN=eth2
	fi

cat << EOF
{
    "changed": false,
    "ansible_facts": {
        "xsce_facts_gathered": true,
        "xsce_wan_iface": "$WAN",
        "xsce_wan_mac" : "$WAN_MAC",
        "xsce_lan_iface": "$LAN",
        "xsce_lan_mac": "$LAN_MAC",
        "xo_version" : "$XO_VERSION"
    }
}
EOF

